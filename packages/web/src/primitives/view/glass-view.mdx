import { Meta, Canvas, Controls, } from '@storybook/addon-docs/blocks';
import * as ViewStories from './glass-view.stories';


<Meta
  of={ ViewStories }
  title='GlassView'
/>


# View


This is the fundamental layout component built around ***flexbox*** to be multi-purpose and open-ended. `Grid`, `Stack`, and `AppShell` are all thin presets over `View`.
<br/>


### View — Glass aesthetic

<Canvas
  of={ ViewStories.GlassView }
  layout='padded'
  source={{
    code: `
    import { SxStyles, Text, View, } from "@lucid-ui/web";

    export const GlassView = () => {
      return (
        <View
          variant={ variants.view.box }
          sx={ styles.box }
        >
          <Text>Skibidi dom yes</Text>
        </View>
      );
    }

    const variants = SxStyles.variants({
      view: {
        box: {
        name: 'glass',
        tone: 'neutral',
        intensity: 'faint',
        elevation: 'level-1',
        blur: 'md',
      },
      },
    });

    const styles = SxStyles.create({
      box: {
        alignItems: 'center',
        borderRadius: '2xl',
        marginHorizontal: 'auto',
        padding: 'space-3',
        width: 'half'
      },
    });
    `,
  }}
/>
<Controls of={ ViewStories.GlassView }/>
