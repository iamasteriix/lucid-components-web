services:
  react:
    build: .
    container_name: nine-ui
    image: nine-ui-react
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
