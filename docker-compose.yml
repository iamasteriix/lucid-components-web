services:
  dev:
    build: .
    container_name: lucid-components-web
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
